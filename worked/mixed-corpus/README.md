# Mixed Corpus Example

Use this when you want a small markdown-first corpus for:

- `beehive ingest ./worked/mixed-corpus`
- `beehive compile`
- `beehive graph serve`
- `beehive graph export --svg ./exports/mixed.svg`

This example mirrors the docs-first and graph-report flow used in the public docs.
