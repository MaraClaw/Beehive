# Capture Example

Use this when you want a small example of the opinionated capture layer:

- `beehive add https://arxiv.org/abs/2401.12345`
- `beehive add https://x.com/...`

This folder exists so the docs and release notes can point at a concrete capture workflow without relying on a larger fixture set.
